{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/django/Dockerfile"
  },
  "deploy": {
    "releaseCommand": "python manage.py migrate --noinput && python manage.py telegram_setup_webhook",
    "restartPolicy": "ON_FAILURE"
  }
}
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "docker/django/Dockerfile"
  },
  "deploy": {
      "startCommand": "sh -c 'python manage.py migrate --noinput && python manage.py collectstatic --noinput --clear && gunicorn config.wsgi:application --bind 0.0.0.0:${PORT} --workers 2 --timeout 120'",
    "restartPolicy": "ON_FAILURE"
  }
}
